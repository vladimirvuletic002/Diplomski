# Build output
bin/
*.exe

# Test / coverage output
*.out
coverage.html

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local secrets (Argo CD repository token) — never committed
.env.local

# Velero S3 credentials for the local MinIO — generated, never committed
k8s/velero/credentials-velero
